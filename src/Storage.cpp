#include "Storage.h"

volatile uint32_t debug_lba;
volatile uint32_t debug_len;
volatile bool debug_flag = false;

Storage::Storage() {
  // constructor body (can be empty)
}

Storage::~Storage() {}

Adafruit_USBD_MSC Storage::usb_msc;

// Callback invoked when received READ10 command.
// Copy disk's data to buffer (up to bufsize) and
// return number of copied bytes (must be multiple of block size)
int32_t Storage::msc_read_callback(uint32_t lba, void *buffer,
                                   uint32_t bufsize) {
  uint8_t const *addr = msc_disk[lba];
  memcpy(buffer, addr, bufsize);

  return bufsize;
}

// Callback invoked when received WRITE10 command.
// Process data in buffer to disk's storage and
// return number of written bytes (must be multiple of block size)
int32_t Storage::msc_write_callback(uint32_t lba, uint8_t *buffer,
                                    uint32_t bufsize) {

  uint8_t *addr = msc_disk[lba];
  memcpy(addr, buffer, bufsize);

  debug_lba = lba;
  debug_len = bufsize;
  debug_flag = true;

  // Serial.printf("WRITE LBA %lu\n", lba);

  // for (int i = 0; i < 16; i++) {

  //   char *data = (char *)msc_disk[i];
  //   data[512 - 1] = '\0';
  //   Serial.println(data);
  // }
  // process_config();
  return bufsize;
}

// Callback invoked when WRITE10 command is completed (status received and
// accepted by host). used to flush any pending cache.
void Storage::msc_flush_callback(void) {
  // nothing to do
}

bool Storage::msc_start_stop_callback(uint8_t power_condition, bool start,
                                      bool load_eject) {
  Serial.printf(
      "Start/Stop callback: power condition %u, start %u, load_eject %u\n",
      power_condition, start, load_eject);
  return true;
}

// Invoked when received Test Unit Ready command.
// return true allowing host to read/write this LUN e.g SD card inserted
bool Storage::msc_ready_callback(void) {
#ifdef BTN_EJECT
  // button not active --> medium ready
  return digitalRead(BTN_EJECT) != activeState;
#else
  return true;
#endif
}

#define README_CONTENTS                                                        \
  "This is TinyUSB MassStorage device demo for Arduino on RAM disk."

uint8_t Storage::msc_disk[DISK_BLOCK_NUM][DISK_BLOCK_SIZE] = {
    //------------- Block0: Boot Sector -------------//
    // byte_per_sector    = DISK_BLOCK_SIZE; fat12_sector_num_16  =
    // DISK_BLOCK_NUM; sector_per_cluster = 1; reserved_sectors = 1; fat_num =
    // 1; fat12_root_entry_num = 16; sector_per_fat     = 1; sector_per_track =
    // 1; head_num = 1; hidden_sectors = 0; drive_number       = 0x80;
    // media_type = 0xf8; extended_boot_signature = 0x29; filesystem_type    =
    // "FAT12   "; volume_serial_number = 0x1234; volume_label = "TinyUSB MSC";
    // FAT magic code at offset 510-511
    {0xEB, 0x3C, 0x90, 0x4D, 0x53, 0x44, 0x4F, 0x53, 0x35, 0x2E, 0x30, 0x00,
     0x02, 0x01, 0x01, 0x00, 0x01, 0x10, 0x00, 0x10, 0x00, 0xF8, 0x01, 0x00,
     0x01, 0x00, 0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
     0x80, 0x00, 0x29, 0x34, 0x12, 0x00, 0x00, 'T', 'i', 'n', 'y', 'U', 'S',
     'B', ' ', 'M', 'S', 'C', 0x46, 0x41, 0x54, 0x31, 0x32, 0x20, 0x20, 0x20,
     0x00, 0x00,

     // Zero up to 2 last bytes of FAT magic code
     },

    //------------- Block1: FAT12 Table -------------//
    {
        0xF8, 0xFF,
        0x00, // The first 2 12bit entries of the FAT linked list are reserved.
              // Therefore the first 3 bytes are reserved.
              // The 2 12 bit values must be 0xF8 and 0xFF

        //     [Entry2  | Entry3]
        0xFF, 0x0F, 0x00 // <<< These 3 bytes represents the 12 bit entries 2
                         // and 3 of the FAT linked list.
        // The README.TXT file first block is 2 (see line 112), therefore to
        // find the next block, we go to this section Entry[2]
        // to read the address of the next block. Since the size of the
        // README.TXT is 65 bytes and less then one sector (512) byte.
        // There is no next block to read therefore the Entry[2] contains the
        // value 0xFFF, which is the EOF.
    },

    //------------- Block2: Root Directory -------------//
    {
        // first entry is volume label
        'A', 'R', 'C', 'A', 'D', 'E', 'P', 'A', 'N', 'E', 'L', 0x08, 0x00, 0x00,
        0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x4F, 0x6D, 0x65, 0x43,
        0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        // second entry is readme file
        'c', 'o', 'n', 'f', 'i', 'g', ' ', ' ', 'j', 's', 'n', 0x20, 0x00, 0xC6,
        0x52, 0x6D, 0x65, 0x43, 0x65, 0x43, 0x00, 0x00, 0x88, 0x6D, 0x65, 0x43,
        0x02, 0x00, sizeof(README_CONTENTS) - 1, 0x00, 0x00,
        0x00 // readme's files size (4 Bytes)
    },

    //------------- Block3: Readme Content -------------//
    README_CONTENTS};

// extremely simple FAT12/FAT32-safe hack:
// just search raw disk for ASCII "config.txt"

const char KEY[] = "config.jsn";

char *Storage::find_config_file() {
  for (int i = 0; i < DISK_BLOCK_NUM; i++) {
    for (int j = 0; j < DISK_BLOCK_SIZE - 10; j++) {
      if (memcmp(&msc_disk[i][j], KEY, sizeof(KEY) - 1) == 0) {
        return (char *)&msc_disk[i][j];
      }
    }
  }
  return nullptr;
}

void Storage::process_config() {
  // static bool dumped = false;
  // if (dumped)
  //   return;
  // dumped = true;

  Serial.println("\n--- MSC RAM DISK DUMP ---");

  for (int i = 0; i < DISK_BLOCK_NUM; i++) {
    bool printable_block = false;

    for (int j = 0; j < DISK_BLOCK_SIZE; j++) {
      uint8_t c = msc_disk[i][j];

      if (c >= 32 && c <= 126) {
        printable_block = true;
        Serial.write(c);
      } else if (c == 0x00 && printable_block) {
        Serial.print("\n");
        printable_block = false;
      }
    }

    if (printable_block)
      Serial.print("\n");
  }

  Serial.println("--- END DUMP ---\n");
}

void Storage::begin() {
  if (!TinyUSBDevice.isInitialized()) {
    TinyUSBDevice.begin(0);
  }
  // Set disk vendor id, product id and revision with string up to 8, 16, 4
  // characters respectively
  usb_msc.setID("Adafruit", "Mass Storage", "1.0");

  // Set disk size
  usb_msc.setCapacity(DISK_BLOCK_NUM, DISK_BLOCK_SIZE);

  // Set callback
  usb_msc.setReadWriteCallback(msc_read_callback, msc_write_callback,
                               msc_flush_callback);
  usb_msc.setStartStopCallback(msc_start_stop_callback);
  usb_msc.setReadyCallback(msc_ready_callback);

  // Set Lun ready (RAM disk is always ready)
  usb_msc.setUnitReady(true);
  usb_msc.begin();
}

void Storage::task() {
  if (debug_flag) {
    debug_flag = false;
    // Serial.printf("WRITE LBA %lu len %lu\n", debug_lba, debug_len);

    // for (int i = 0; i < DISK_BLOCK_NUM; i++) {
    //   char *data = (char *)msc_disk[i];
    //   // if (debug_lba == 6) {
    //   data[DISK_BLOCK_SIZE - 1] = '\0';
    //   Serial.println(data);
    //   // }
    // }

    process_config();
  } else {
    // Serial.println("nope");
    // delay(500);
  }
}
